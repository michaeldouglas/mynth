# Mynth Dataset Ownership

Dataset sources are immutable. Derived records must identify the source,
language-specification revision, transformations, split identity, hashes, and
readiness state. Use the `mynth-dataset-preparation` skill.
