# Mynth Training Configurations

Store reviewed configuration metadata and manifests here. A configuration must
identify the model, language specification, dataset revision, strategy,
resources, seed, output policy, and authorization state. Do not store model
weights or checkpoints here.
