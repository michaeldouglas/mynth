# Mynth Artifact Policy

Keep manifests and small metadata reviewable. Keep model weights, checkpoints,
and other heavy outputs in the approved external artifact root. Never overwrite
prior experiment artifacts.
