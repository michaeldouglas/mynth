# Mynth External Representation

The model-facing representation is an experimental external contract. Its
syntax and encoding must be selected by evidence and versioned independently
from Mynth IR and LLVM IR.
